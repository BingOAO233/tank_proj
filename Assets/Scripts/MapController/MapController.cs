using UnityEngine;

public class MapController : MonoBehaviour
{
    public int mapMaxPlayerNum;
    public Transform[] spawnPoints;
    // Start is called before the first frame update

    // Update is called once per frame
    
}
